# Historical selective/half research environment; not GUI/runtime dependencies.
# Install alongside requirements-ml.txt in an isolated research environment.
# Saved CPU-library versions are pinned; choose the supported Torch GPU build
# separately. Historical GPU training used torch 2.11.0+cu128 on RTX 5080.
# Version pins/seeds do not promise bit-identical fitting across GPU drivers.
numpy==2.5.3
catboost==1.2.10
lightgbm==4.6.0
scipy==1.17.1
