# DockDack 작업 지침

## 병합 시 문서 동시 갱신

사용자가 병합을 요청하면 별도 재요청 없이 다음을 같은 작업에 포함한다.

- `docs/FEATURES.md`: 현재 기능, 화면 위치, 동작 조건과 제한을 실제 구현에 맞게 갱신한다. 기존 항목을 수정하고 중복 항목은 만들지 않는다.
- `DEVLOG.md`: 날짜, 출발·대상 브랜치, 변경 요약, 실제 검증 결과와 남은 제한을 기록한다.
- 기능 변경이 없는 병합도 기능서와 구현의 일치 여부를 확인하고 개발일지에 병합 내용을 남긴다.
- 실행하지 않은 검증이나 아직 완료하지 않은 병합·푸시는 완료로 기록하지 않는다.

병합 대상은 사용자가 지정한 브랜치로 한정한다. 이 문서 갱신 규칙은 자동매매 시작·종료, 주문 전송, 운영 DB 변경이나 다른 브랜치 병합을 허용하지 않는다. 비밀키, `.env`, 운영 장부, IDE 설정과 임시 산출물은 커밋하지 않는다.
