# MK1 · MK1.1 실제 학습 방식과 저장 모델 명세

확인일: 2026-09-24 · 브랜치: `mark_1` · 앱: **0.1 (MK1)**

이 문서는 **현재 GUI에 연결되는 저장 모델**을 학습 코드, 모델 manifest, 당시 실행 결과와 대조한 기록이다. 새로 학습하거나 백테스트한 결과가 아니다. 앱 버전 0.1, 모델 이름 MK1/MK1.1, 학습 실행 버전은 서로 다른 식별자다.

## 1. 먼저 결론

현재 MK1과 MK1.1은 모두 **CatBoost 결정트리 앙상블**이다. RNN·LSTM·ResNet도 이전에 실험했지만 현재 연결된 두 prototype의 내부 모델은 신경망이 아니다. CatBoost는 이전 트리의 오차를 보완하는 트리를 순차적으로 학습하는 그래디언트 부스팅 기법이다.

| 구분 | MK1 | MK1.1 |
|---|---|---|
| GUI 이름 | `mark1 prototype` | `mark1.1 prototype` |
| 독립 출처 ID | `mark1-prototype-demo-trigger` | `mark1-1-prototype-demo-trigger` |
| 저장 폴더 | `models/mark1_prototype/` | `models/mark1_1_prototype/` |
| 원 학습 실행 | `selective-20260916` | `half-20260920` |
| 학습 저장 버전 | `20260916-v1` | `20260920-v1` |
| 별도 이름 부여 | 원 prototype | `20260924-v1` — 원본 `mark1_0504`의 이름·출처만 분리 |
| 익절 / 손절 | +1% / −0.9% | +0.5% / −0.4% |
| 국내 구조 | CatBoost 4분류, 깊이 6 | 같은 구조로 새 목표 재학습 |
| 미국 구조 | CatBoost 이진분류, 깊이 8 | 같은 구조로 새 목표 재학습 |
| 시장별 앙상블 | 시드 42·43·44의 3개 모델 | 시드 42·43·44의 3개 모델 |
| 입력 | 완료된 과거 30봉 OHLCV + 후보 진입가 | 동일 |
| 최종 학습의 후보 진입가 | 실제 당일 시가 | 실제 당일 시가 |
| 최종 학습의 가상 가격 증강 | **사용하지 않음** | **사용하지 않음** |
| 매수 확률 문턱 | 보정 성공확률 **50% 초과** | 동일 |
| 현재 승인 상태 | 연구 기준 미통과·모의 전용 | 연구 기준 미통과·모의 전용 |

근거: [MK1 저장 계약](models/mark1_prototype/manifest.json), [MK1.1 이름/출처 계약](models/mark1_1_prototype/alias.json), [0.5/0.4 원 학습 계약](models/mark1_0504/manifest.json).

**‘현재가를 바꿔 질문할 수 있다’와 ‘다양한 현재가로 증강학습했다’는 다르다.** 초기 신경망 비교에서는 가격 증강을 실험했지만, 현재 선택된 두 CatBoost 학습에서는 실제 시가 표본만 사용했다. 31번째 입력도 완성된 당일 봉이 아니라 후보 진입가격 하나다.

## 2. 공통 데이터와 특징 생성

### 2.1 어떤 DB를 사용했나

`data/kiwoom_daily/clean-20260916-v1/` 아래 국내·미국 정제 SQLite DB가 원천이다. 정제 기준 시각은 `2026-09-16` 미만이며 시장별 실제 마지막 수집일은 다르다. 학습기는 승인된 `training_samples`의 30일 입력창과 해당 목표일 OHLC를 읽었다. DB에 있던 ‘다음 종가 +1%’ 정답을 재사용하지 않고 각 모델의 **고가·저가 장벽 정답을 새로 계산**했다.

정제에는 거래일 연속성, OHLCV 유효성, 가격 기준 분절, 과거 활동성·거래량·거래대금 필터가 포함된다. 예를 들어 20일 중앙 거래량 최소 1만 주, 중앙 거래대금 국내 10억 원/미국 100만 달러, 과거 60일 활동비율 최소 95% 등의 기준이다. 이는 유통주식 수를 직접 확보해 저유통주를 판별한 것이 아니라 **거래 활동성 기반의 저유동성 필터**다. 미래 결과를 보고 종목을 골랐다는 뜻도 아니다.

원본 DB를 덮어쓰지 않고 읽기 전용 DB·해시가 붙은 원시 OHLCV 캐시를 사용했다. 초기 캐시 설명에 남은 `max_train_samples=200000`은 최종 CatBoost 학습량이 아니다. 실제 최종 표본 수는 아래 실행별 분할 표를 기준으로 한다. 과거 기록의 `dockdack-data-collection`, `dockdack-mark_1` 절대경로는 이전 폴더의 출처 기록이며 현재 통합 경로와 구분한다.

근거: [정제 기준](docs/dataset-cleaning.md), [MK1 데이터 생성](dockdack/mark1_data.py), [기간 분리](dockdack/mark1_deep_data.py), 각 번들의 `source`와 로컬 실행의 `splits.json`/`metadata.json`.

### 2.2 30봉을 184개 숫자로 변환

두 모델 모두 봉 이미지를 보거나 RNN에 31개 봉을 넣는 방식이 아니다. 과거 30봉과 후보가격을 아래 **184개 통계 특징**으로 계산한 뒤 트리에 입력한다.

| 특징 묶음 | 개수 | 내용 |
|---|---:|---|
| 5·10·20·30일 창별 통계 | 128 | 수익률·변동성·상하방 RMS·범위·true range·몸통/꼬리·과거 장벽 도달 비율·거래량·추세 효율 등, 창당 32개 |
| 수익률 lag | 7 | 1·2·3·5·10·20·29일 |
| 최근 봉 형태 | 24 | 1·2·3·5일 전 봉의 형태, 각 6개 |
| 후보가·과거 상태 | 13 | 직전 종가 대비 gap, 과거 변동성과 추세 등 |
| 후보가의 과거 가격대 위치 | 12 | 4개 창의 평균·고점·저점 대비 거리 |

특징 계산에는 목표일 최종 고가·저가·종가·거래량, 종목 ID, 날짜 자체를 넣지 않는다. 각 표본의 과거만 사용하고 전체 기간 평균·표준편차로 정규화하지 않는다. 내부 계산은 float64, 출력은 float32이며 고정 범위 `[-20, 20]`로 제한한다. 가격×거래량은 대용 지표이고 실제 체결 거래대금이나 유통주식 수와 같지 않다.

MK1.1에서는 이 중 **과거 장벽 도달 비율 16개**도 +0.5/−0.4 기준으로 다시 만들었다. 최종 매도 숫자만 바꾼 모델이 아니다.

근거: [공통 특징 정의](dockdack/mark1_selective_features.py), [MK1.1 특징·정답 재생성](dockdack/mark1_0504_data.py). 전체 특징명과 순서는 각 번들의 `feature_names`에 보존된다.

## 3. MK1 — +1% / −0.9%

### 3.1 무엇을 정답으로 학습했나

진입가격을 `P`라고 할 때 성공 정답은 다음과 같다.

```text
당일 고가 >= P × 1.01  AND  당일 저가 > P × 0.991
```

익절선에 닿고 손절선에는 닿지 않아야 성공이다. 당일 저가가 손절선에 정확히 닿아도 실패이며 양쪽을 모두 찍으면 **손절 먼저인 실패**다. 구현에서는 부동소수점 경계 비교의 미세 오차를 처리한다.

국내 모델은 `익절만 / 손절만 / 양쪽 도달 / 둘 다 미도달`의 4개 상태를 분류한다. 미국 모델은 `익절만`을 성공으로, 나머지 3개 상태를 실패로 묶은 이진 분류다. 따라서 미국 모델에는 별도의 손절 확률 출력이 없다.

이는 ‘하루 전체에서 손절선을 전혀 안 건드리면서 익절선 도달’ 사건이다. **장중 특정 시점에 진입한 뒤 익절이 먼저 올 확률을 관측·검증한 정답이 아니다.** 일봉에는 당일 고점과 저점의 발생 순서 및 시각이 없기 때문이다.

### 3.2 후보 비교와 최종 선택

후속 selective 실험에서는 `cat_binary6`, `cat_binary8`, `cat_joint6`, `lgbm_binary` 네 후보를 비교했다. CatBoost는 GPU, LightGBM은 CPU로 학습했다. 4후보 × 2개 시간 구간 × 2시장 × 첫 시드 = 16회 비교 후, 선정 구조에 시드 43·44를 두 구간 모두 추가해 총 24회 학습했다. 가장 좋은 시드 하나를 고르는 방식은 아니다.

신호 표본이 충분한 경우 두 개발 구간 중 낮은 성공률 신뢰구간 하한, 비용 후 수익 하한, 신호 수 등을 우선했다. 증거가 부족한 경우에는 확률 오차 지표인 Brier score로 진단용 후보를 선정했다. 미국의 `cat_binary8`도 충분한 매수 신호 증거가 없어 이 대체 기준으로 선택됐다. **후보 중 선택됨과 수익성 연구 기준 통과는 별개**다.

최종 저장본은 `walk_2024`의 국내 `cat_joint6`, 미국 `cat_binary8`이다. 이전 `walk_2022`는 비교·검증용이며 현재 추론 시 함께 섞지 않는다.

근거: [학습 실행 코드](examples/train_mark1_selective.py), [선별 정책](dockdack/mark1_selective_policy.py), [선별 실험 정의](docs/mark1-selective-protocol.md).

### 3.3 실제 저장 모델의 학습 기간·양

| 역할 | 국내 날짜 / 표본 수 | 미국 날짜 / 표본 수 |
|---|---|---|
| 가중치 학습 | 2014-01-02~2021-12-30 / **1,489,229** | 2014-01-02~2021-12-31 / **1,500,000** |
| 조기 종료용 검증 | 2022-02-17~12-29 / 150,000 | 2022-02-15~12-30 / 150,000 |
| 확률 보정 | 2023-02-15~06-30 / 78,595 | 2023-02-15~06-30 / 233,990 |
| 매수 정책 선택 | 2023-08-14~12-28 / 67,556 | 2023-08-15~12-29 / 239,640 |
| 개발 감사 | 2024-02-15~12-30 / 144,599 | 2024-02-14~12-31 / 588,175 |

학습 종목 수는 국내 2,043개, 미국 3,433개다. 위 숫자는 **종목들을 합친 종목·날짜별 입력창 수**이며 종목당 개수, 독립 관측 수, 모델 파라미터 수가 아니다. 시드 3개는 같은 학습 표본을 다시 사용한다.

보조 `walk_2022`는 2012~2019 학습 → 2020 종료 검증 → 2021 상반기 보정/하반기 정책 → 2022 감사다. 두 구간 모두 후속 연도·반기 경계에서 입력 과거 30거래일이 이전 구간과 겹치는 표본을 제거한다. 학습·종료 검증 상한은 각각 150만·15만이며 정답과 무관한 고정 시드 무복원 추출이다. 다른 단계는 적격 표본 전체를 사용한다.

실행 근거: 로컬 `outputs/mark1/selective-20260916/{domestic,us}/walk_2024/splits.json` 및 각 trial의 `metadata.json`. 이 대용량 실행 폴더는 Git 제외이며, 공개 보존 근거는 [국내 manifest](models/mark1_prototype/domestic/manifest.json), [미국 manifest](models/mark1_prototype/us/manifest.json)와 학습 코드다.

### 3.4 실제 학습 설정·확률 보정

| 설정 | 국내 | 미국 |
|---|---|---|
| 라이브러리·장치 | CatBoost 1.2.10 / GPU | 동일 |
| 손실 함수 | MultiClass | Logloss |
| 조기 종료 평가 | MultiClass loss | PR-AUC (`PRAUC:type=Classic`) |
| 깊이 | 6 | 8 |
| 학습률 / L2 | 0.03 / 10 | 동일 |
| 부스팅 / 샘플링 | Plain / Bernoulli, subsample 0.8 | 동일 |
| 수치 특징 분할 수 | border count 128 | 동일 |
| 최대 반복 / patience | 3,000 / 개선 없는 150회 | 동일 |
| 최종 보존 round 수, 시드 42/43/44 | **2,945 / 3,000 / 2,986** | **149 / 165 / 165** |

이 숫자는 신경망 층 수나 epoch 수가 아니라 트리 부스팅 반복 수다. 클래스 가중치나 성공 표본 과다추출은 사용하지 않았다. GPU 학습 기록과 별개로, 현재 모델 로딩·추론은 CPU 경로를 사용한다.

각 모델의 성공 **원시 log-odds를 평균한 뒤** 별도 2023년 상반기에서 학습한 단조 Platt 보정 `sigmoid(a × 평균점수 + b)`를 적용한다. 단순 확률 평균이 아니다. 국내는 `손절만 + 양쪽 도달`의 log-odds도 별도로 보정하며 두 사건이 독립이라고 가정해 확률을 곱하지 않는다.

저장 정책은 두 시장 모두 `p > 0.5`, 손절 확률 상한은 `1.0`이다. 따라서 국내 손절 확률이 출력되더라도 현재 별도의 유효한 추가 필터로 작동하지는 않는다. 정확히 50%는 매수 신호가 아니다.

근거: [학습/예측 구현](dockdack/mark1_selective_models.py), [Platt 보정](dockdack/mark1_metrics.py), [MK1 추론](dockdack/mark1_prototype_inference.py), 시장별 manifest의 `members`, `calibration`, `policy`.

## 4. MK1.1 — +0.5% / −0.4%

### 4.1 무엇을 새로 학습했나

MK1.1의 원본은 2026-09-20에 학습한 `mark1_0504`다. 정답을 아래로 바꾸고, 목표 의존 특징 16개도 다시 계산한 뒤 **모든 선택 모델을 새로 학습**했다.

```text
당일 고가 >= P × 1.005  AND  당일 저가 > P × 0.996
```

MK1과 동일하게 양쪽 도달은 손절 먼저인 실패다. 목표를 낮췄다고 가상 가격 증강을 새로 넣지는 않았다. 실제 당일 시가만 사용했다. 국내 4분류 깊이 6, 미국 이진 깊이 8은 이전 실험의 선정 구조를 사전에 고정했다. **이번 재학습에서 RNN·ResNet·다른 트리를 다시 경쟁시킨 것은 아니다.**

2시장 × 2기간 × 3시드 = **12개 모델**을 학습했고, 최종 번들에는 최신 `walk_2024`의 6개만 저장했다. 2026-09-24 `mark1.1 prototype`이라는 이름을 붙일 때는 추가 학습이나 확률 변경 없이 식별자·출처를 분리했다.

근거: [재학습 코드](examples/train_mark1_0504.py), [새 정답·특징](dockdack/mark1_0504_data.py), [이름 분리 검증](models/mark1_1_prototype/alias-validation.json).

### 4.2 데이터에서 달라진 점

미국 FCEL·BNED·BBSI는 분할·병합 관련 가격 기준 불연속이 확인돼 학습과 평가 전체 기간에서 종목 단위로 격리했다. **DB 값을 수정한 것은 아니다.** 3,586 + 2,064 + 3,341 = 8,991개 창을 제외했다.

| 시장 | 정제 캐시 창 수 | MK1.1 격리 후 허용 창 수 | 허용 종목 수 |
|---|---:|---:|---:|
| 국내 | 2,668,151 | 2,668,151 | 2,049 |
| 미국 | 8,088,544 | 8,079,553 | 3,447 |

위 숫자는 전체 허용 캐시이지 한 모델의 train 개수가 아니다. MK1은 격리 전 학습, MK1.1은 격리 후 학습이므로 두 결과 차이를 익절·손절 폭 변경 하나의 효과라고 단정할 수 없다. SONY의 잔여 가격 단위 이상과 미진단 기업행사 위험도 남는다.

### 4.3 기간·학습 설정·보정

시간 분리 방식과 국내 날짜/표본 수는 MK1과 같다. 미국의 격리 후 최신 구간은 다음과 같다.

| 역할 | MK1.1 미국 `walk_2024` 날짜 | 표본 수 |
|---|---|---:|
| 학습 | 2014-01-02~2021-12-31 | 1,500,000 |
| 조기 종료용 검증 | 2022-02-15~12-30 | 150,000 |
| 확률 보정 | 2023-02-15~06-30 | 233,802 |
| 추가 검증 | 2023-08-15~12-29 | 239,448 |
| 개발 감사 | 2024-02-14~12-31 | 587,581 |

MK1과 달리 이 재학습에서는 `p > 0.5` 정책을 고정해 추가 검증 기간에 문턱을 검색하지 않았다. 상반기 Platt 보정, 최신 구간 저장, 30거래일 중복 제거는 유지했다. 네 시장·기간의 정확한 표본 수는 [원 재학습 보고서](reports/mark1-0504-20260920/REPORT.md)에 있다.

CatBoost 1.2.10, GPU, 깊이 6/8, 학습률 0.03, L2 10, Plain, Bernoulli 0.8, border count 128, 최대 3,000회·patience 150, 시드 42/43/44는 MK1과 같다. 최신 모델의 보존 round 수는 국내 **2,951 / 3,000 / 2,976**, 미국 **497 / 1 / 977**이다. 미국 시드 43의 `1`은 실제 저장된 조기 종료 결과이며 문서 오타나 1층 신경망이 아니다.

세 모델의 원시 성공 log-odds 평균 뒤 별도 Platt 보정을 적용한다. 최신 보정 계수는 국내 `a≈0.806998, b≈−0.394479`, 미국 `a≈1.476276, b≈−0.023891`이다. 비가중 이진 교차엔트로피·양수 기울기 조건으로 보정하며, 미국에는 별도 손절 head가 없다. 연구 판정과 관계없이 현재 prototype 신호 문턱은 엄격한 `p > 0.5`다.

근거: [국내 저장 모델](models/mark1_0504/domestic/manifest.json), [미국 저장 모델](models/mark1_0504/us/manifest.json), 로컬 `outputs/mark1/half-20260920/`의 trial `request.json`/`metadata.json`.

## 5. 실제로 저장된 평가 결과

아래는 **실제 계좌 수익률이 아닌 역사 백테스트**다. 국내 2025-02-19~2026-08-21(368거래일), 미국 2025-02-18~2026-09-11(394거래일)을 평가했다. 이 기간은 앞선 연구에서 이미 확인했으므로 새로운 미사용 최종 시험이라고 부를 수 없다.

동일한 격리 후 평가 사건에서 비교한 결과다. 국내 261,385개 창, 미국 1,076,555개 창이며, MK1 원 저장 평가의 미국 1,077,696개와 구분한다.

| 모델·시장 | p>0.5 신호 수 | 신호 성공률 | 종가 청산 거래 수 | 비용 후 총수익률 | 최대낙폭 |
|---|---:|---:|---:|---:|---:|
| MK1 국내 | 441 | 49.89% | 439 | −2.69% | −3.21% |
| MK1 미국 | 0 | 산출 불가 | 0 | 0%, 무거래 | 0%, 무거래 |
| MK1.1 국내 | 2 | 50.00%, 표본 부족 | 2 | −0.0127% | −0.0276% |
| MK1.1 미국 | 123 | 57.72% | 123 | −0.1777% | −0.3703% |

목표 미도달 시 다음 날로 보유하는 방식에서는 MK1 국내 438거래/−2.30%, MK1.1 미국 121거래/−0.2800%였다. MK1.1 국내는 2거래로 결과가 동일하다. **신호 수·신호 성공률·포트폴리오 거래 수·수익 난 거래 비율은 다른 통계**다. 비신호가 많아서 높은 전체 분류 정확도를 매수 성공률이라고 쓰면 안 된다.

백테스트 가정은 다음과 같다.

- 국내 초기 1천만 원, 미국 1만 달러. 매수만·레버리지 없음·최대 20보유·종목당 당일 시작 평가자산 5%.
- 과거 20일 거래량 중앙값의 0.1%까지 정수주, 종목당 하루 한 번 실제 시가 진입 가정.
- 익절/손절 모두 닿으면 손절 먼저. 목표 미도달은 다음 날 보유 또는 당일 종가 청산을 나눠 비교.
- 왕복 비용 **20bp=0.20%**를 매수·매도 금액에 절반씩 부과한 연구 가정. 실제 계좌 수수료·세금의 정확한 요율이라는 뜻이 아니다.
- 장중 매도대금으로 같은 날 시가 매수를 소급 실행하지 않음. 호가 스프레드·시장충격·부분체결·가격제한·체결 지연을 별도로 재현하지 않음.

백테스트의 ‘당일 종가 청산’은 앱의 **마감 5분 전 주문**과 다르며 그 주문의 접수·체결을 검증한 수치가 아니다. 비용을 0으로 놓으면 MK1.1 미국 종가 청산 수익률은 +1.03%지만, 20bp 적용 시 −0.18%다. 비용 후 이익 모델이 완성됐다고 해석할 수 없다.

근거: [MK1.1과 기존 모델의 동일 사건 비교·비용 민감도 전체 보고서](reports/mark1-0504-20260920/REPORT.md), MK1 시장별 manifest의 `research_results.reused_evaluation`. MK1.1 manifest에는 개발 구간 결과만 있으므로 위 2025년 이후 수치는 비교 보고서를 기준으로 한다.

### 목표를 좁혔는데 왜 신호가 항상 늘지 않나

같은 2024년 개발 감사 창에서 MK1→MK1.1로 바꾸면 국내 양쪽 장벽 도달 비율은 **38.04%→63.96%**, 미국은 **23.45%→53.11%**로 늘었다. 손절 우선 규칙 때문에 성공 정답 비율은 국내 25.44%→14.73%, 미국 28.35%→19.94%로 줄었다. 따라서 익절 폭 축소가 매수 신호 증가나 성공률 상승을 보장하지 않는다.

## 6. 과거 딥러닝 실험과 현재 모델의 관계

| 단계 | 실제 실험한 기술 | 당시 선정 결과 | 현재 prototype에 포함? |
|---|---|---|---|
| 초기 mark1 | MLP, LSTM, GRU, TCN, Transformer, 가격 증강 없는 MLP 대조군 | 두 시장 `mlp_no_price_aug` | 아니오 |
| 심화 연구 | Deep MLP, 1D ResNet-18/34 계열, Inception 계열 | 국내 Inception, 미국 ResNet-34 | 아니오 |
| selective 연구 | CatBoost 3종, LightGBM | 국내 `cat_joint6`, 미국 `cat_binary8` | **MK1의 실제 구성** |
| 0.5/0.4 연구 | 선정 CatBoost 구조 고정 후 새 정답·특징으로 재학습 | 동일 구조의 새 가중치 | **MK1.1의 실제 구성** |

초기 실험에서는 실제 시가의 `[0.99, 0.995, 1, 1.005, 1.01]` 후보가격을 만들고 각각 정답을 다시 계산했다. 이는 실제 장중 거래 5건을 관측한 것이 아니라 반사실적 가격 질문 5개다. 최종 선택된 CatBoost 실험에는 이 증강을 사용하지 않았다. 심층 실험 또한 연구 기준을 통과하지 못했고, 층을 깊게 만들었다는 사실이 성공률·수익성 개선을 증명하지 않는다.

근거: [초기 실험](docs/mark1-strategy.md), [심층 실험 설계](docs/mark1-deep-protocol.md), [초기 결과](reports/mark1-20260916/REPORT.md), 로컬 `outputs/mark1/experiment-20260916/` 및 `deep-20260916/summary.json`.

## 7. GUI에서 매수·매도하는 방식과 남은 한계

외부 모델은 현재 완료 30봉과 후보 현재가로 성공확률을 다시 계산해 **매수 후보 신호만** 반환한다. 주문 전 새 현재가·지정가에서 다시 확인하며 계정·신호 유효성·수량/금액 한도·미확정 주문·모델 보유분 검사를 별도로 통과해야 한다. 확률만 높다고 반드시 주문되는 것은 아니다.

매도는 별도 신경망을 학습한 결과가 아니다. 원매수 모델별 보유분의 **확인된 평균 체결가** 대비 MK1 +1/−0.9%, MK1.1 +0.5/−0.4%라는 가격 규칙이다. 두 모델이 같은 종목을 각각 사도 보유분과 원가를 나눠 기록한다. 마감 청산은 이와 별도의 앱 정책이다. [현재 GUI 연결·보유 계약](docs/MARK1_1_PROTOTYPE.md), [최신 기능서](docs/FEATURES.md)를 참고한다.

두 번들 모두 `research_only=true`, `research_qualified=false`, `deployment_allowed=false`, `intraday_path_verified=false`다. 현재 연결은 **모의 실험 경로**이며 실전 승인·수익성 입증이 아니다.

MK1 연구 기준에는 최소 신호량, 관측 성공률 65% 이상, 성공률 95% 하한 57.9% 초과, 왕복 20bp 후 수익 하한 양수 등이 있었다. MK1.1은 최소 50신호·20신호일·10종목과 성공률 95% 하한 **2/3 초과**, 비용 후 수익 하한 양수를 요구했다. 10거래일 블록을 2,000회 재표집해 구간을 계산한다. 2/3은 +0.5/−0.4와 비용 0.2%의 두 결과만 놓은 단순 손익분기 기준이며 미도달 종가 수익이 있으면 달라진다. **두 모델 모두 연구 통과 모델이 아니다.**

일봉만으로 임의 장중 진입 이후의 경로·선후 순서를 복원할 수 없고, 과거 30봉 입력과 목표일까지의 기업행사·가격 기준 무결성이 완전히 인증된 것도 아니다. MK1 미국의 무신호/무손실은 좋은 예측력의 증거가 아니며 MK1.1 국내 2신호의 50%도 신뢰할 만한 성공률 추정이 아니다. 해시 검사·원본과 내보낸 모델의 추론 일치는 저장 무결성 검증이지 실제 수익 보장이 아니다.

## 8. 기록과 재현 경계

현재 배포 모델·특징 순서·보정 계수·시드·위험 표시는 위 `models/` manifest에서 확인한다. 상세 학습 실행 기록은 로컬 `outputs/mark1/selective-20260916/`, `half-20260920/`, 비교 결과는 `half-backtest-20260920/`에 있다. 대용량 로컬 산출물은 Git에 포함하지 않는다.

과거 절대경로와 봉인된 소스를 임의로 고치거나 실행 폴더에 덮어쓰지 않는다. 재실행이 필요하면 [연구 호환 도구](docs/research-tools.md)를 사용해 동일 내용 해시를 대조하고 새 출력 폴더를 지정한다. 이 문서를 만들면서 재학습·새 백테스트·계좌 조회·주문·원본 데이터 변경은 하지 않았다.
